def compute(in1, in2):
    """A textual description of the compute function.

    Inputs:
        src_prog (all): Textual description of in1
        in2 (all): Textual description of in2

    Outputs:
        dot (all): Textual description of out1
        YAML (all): Textual description of out2

    Requirements:
    """
    # some code
    out1 = src_prog
    out2 = "This is the in2 string:" + in2

    return {"dot": out1, "YAML": out2}


def test():
    """Test the compute function."""

    print("Running test")
