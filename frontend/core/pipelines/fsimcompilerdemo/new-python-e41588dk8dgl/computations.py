def compute(src_prog):
    """A textual description of the compute function.

    Inputs:
        src_prog : The python program to be compiled

    Outputs:
        dot (all): The dot file showing the TiDaL Graph
        YAML (all): The Fsim Yaml program file to be simulated

    Requirements:
    """
    dot = "The (LL) compiler needs to run here"
    fsim_program = "Custom Yaml File to Come"

    return {"dot": dot, "YAML": fsim_program}


def test():
    """Test the compute function."""

    print("Running test")
